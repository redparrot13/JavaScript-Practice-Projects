{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "My first weather app",
  "main": "app.js",
  "scripts": {
    "start": "live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Katie",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
